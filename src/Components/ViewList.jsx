import { render } from '@testing-library/react';
import React, { Component } from 'react';

export default ViewList extends Component {
    render() {
        return(
             'vista de lista'
        )
    }
}